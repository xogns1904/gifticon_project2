<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.company.gifticon_project3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gifticon_Project3">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gift_giving_Activity" android:label="선물 주기 액티비티"/>
        <activity android:name=".gift_clear" android:label="선물 확인 액티비티2" />
        <activity android:name=".gift_giving2_Activity" android:label="선물 주기 액티비티"/>
        <activity android:name=".gift_clear2" android:label="선물 확인 액티비티2"/>
    </application>

</manifest>